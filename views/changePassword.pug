extends layouts/main.pug

block content
    .form-container
        form(action=routes.changePassword, method="post")
            input(type="password", placeholder="Current Password", name="crtPwd")
            input(type="password", placeholder="New Password", name="newPwd")
            input(type="password", placeholder="Verify New Password", name="vefPwd")
            input(type="submit", value="Change Password")